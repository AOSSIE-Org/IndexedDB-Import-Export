// TODO: Browser file utilities — trigger JSON download, read file input
//
// Responsibilities:
// - downloadJSON(data, filename): Create a Blob from JSON data and trigger a browser download
// - readJSONFile(file): Read a File object (from <input type="file">) and parse it as JSON
// - These are convenience wrappers — the core library works without them
